import { EditorShell } from "@/components/editor/EditorShell";

export const metadata = {
  title: "mos.bi — Редактор дашборда",
};

export default function DashboardEditorPreviewPage() {
  return <EditorShell />;
}
